{
  "name": "gates-inbound-shipments",
  "version": "1.0.0",
  "description": "GEL Inbound Shipments Board — raw-material shipments expected within ±30 days, not yet arrived",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev":   "nodemon server.js"
  },
  "dependencies": {
    "axios":   "^1.7.2",
    "dotenv":  "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
